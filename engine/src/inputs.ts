import { Key } from "game-lib/utils/keycode.js";

export class Input {
    private static pressedKeys: Set<Key>;
    public static isKeyDown(key: Key) {

    }

    public static onKeyDown(key: Key) {}
}